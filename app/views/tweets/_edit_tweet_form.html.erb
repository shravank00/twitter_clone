<%= form_with(model: tweet, class: 'edit-tweet-form', data: { turbo: false }) do |form| %>
  <%= form.rich_text_area :content %>
  <%= form.submit 'Update Tweet', class: 'btn btn-primary' %>
<% end %>